# 更新记录

## V4.1.6 — 2026-08-22

- 新增轻量检查更新：点击左上角版本号，可查看当前版本与最新版本；
- 启动后每 24 小时最多静默检查一次，只读取公开 `version.json`，不上传文章、标题、作者或草稿；
- 新版本提示先引导导出项目备份，离线 HTML 只提供下载入口，不会自动覆盖当前文件；
- 增加固定在线地址和固定最新下载文件名，以后分享同一个地址即可；
- 网络失败或断网时不影响编辑、自动保存、排版、预检和复制。

## V4.1.5 — 2026-08-22

- 将一键排版解析器升级为内嵌、固定版本的本地 Markdown Token 解析，不依赖 CDN；
- 新增代码块与公式块，组件总数从 31 升级为 33；
- 围栏代码逐字符保留缩进、Tab、空行、特殊符号和尾随换行，代码里的 Markdown/HTML 不会被误识别或执行；
- 支持行内代码、行内公式、链接、Markdown 图片、任务清单、分割线和可变列表格；
- 公式完整保留 LaTeX，并进入公式图片清单；公众号稳定复制默认阻断未处理公式，避免静默变成普通文字；
- 修复真实生成时把公式方括号、链接 URL、纵向表格标签误判为“原文被删除”的问题；
- 章节标题同时识别半角 `|` 与全角 `｜` 编号前缀，避免自动编号后重复显示；
- 未闭合代码围栏或块公式会显示原始行号并停止生成，当前草稿不会被修改；
- 免费版、创作者工作台版与 GitHub 在线版共用同一解析和微信发布内核。

## V4.1.4 — 2026-08-21

- 一键排版在没有品牌文末时，自动添加通用三连提示、关注提示和感谢页脚；
- 已有明确固定文末或定制文末时优先原样保留，不重复追加；
- “连同固定品牌文末一起替换”仍可生成真正无落尾的文章；
- 追加模式不自动插入落尾，避免合并文章时把文末夹在中间；
- 预览摘要和完成提示会明确说明本次是保留现有文末、添加通用落尾，还是不添加落尾。

## V4.1.3 — 2026-08-20

- 修复“一键排版只有普通正文、缺少卡片和背景”的结构断层；
- 新增版式配方层，按文章内容自动组装导语、重点、方法、总结和结尾；
- 默认使用均衡版式，可在“更多设置”切换清爽或丰富；
- 默认只替换正文并保留已定制的文末组件，仍可显式整篇替换；
- 生成前后执行原文完整性检查，发现删除、重复或顺序变化时停止修改；
- 修复 `**摘要：**`、`**关键词：**` 被误识别为列表的问题。

视觉主题负责颜色与标题风格，版式配方负责内容区块结构；两者现已分开工作。微信最终效果仍需完成粘贴、保存草稿、重新打开和手机预览。

## V4.1.2 — 2026-08-20

- 新增“一键排版文章”：读取剪贴板失败时可手动粘贴；所有处理均在当前浏览器完成；
- 默认替换上次草稿，可选追加到文末，整次生成可一步撤销；
- 自动识别文章标题、Markdown 小标题、段落、引用、列表、强调句和明确步骤；
- 自动判断教程实操、观点文章、故事复盘、清单盘点或通用文章，并匹配安全主题；
- 空白页不再是白屏死路；恢复旧草稿时明确显示区块数量，并提供“新文章一键排版”；
- 明确区分“排版样式（不改正文）”与“新建文章骨架（会替换正文）”。

仍需在微信后台完成：粘贴、保存草稿、重新打开和手机预览。

## V4.1.1 — 2026-08-20

首次 GitHub 开源发布：

- 35 套主题、16 套文章结构与 31 个内容区块；
- 微信独立发布渲染器，不直接复制编辑器状态；
- 非数据组件不使用表格模拟布局；
- 图片与链接清单、真实表格高风险提示；
- 静态发布预检、清洁富文本复制；
- 本地草稿、快照和项目 JSON 备份；
- 物理移除内部 QA、授权生成器和所有非公开版本数据；
- 新增 GitHub Pages 在线体验与 MPL-2.0 开源许可。

本地测试不等于微信最终验收。正式发布前仍需完成粘贴、保存草稿、重新打开和手机预览。
