# 更新记录

## V4.1.10 — 2026-08-26

- 发现正式新版后显示持续可见、不会遮挡编辑的更新横幅，不再只依赖短提示和版本小圆点；
- 版本徽标会直接显示“可更新 Vx”，横幅提供在线最新版、离线 ZIP 和“稍后提醒”入口；
- 使用带旧版本参数的在线链接时，明确提示当前页面已经加载最新版，并引导改用固定在线地址；
- 远端清单标记为候选版时不触发更新提醒，也不消耗正式版提醒记录；
- 最新版、离线状态和版本检查失败时不显示横幅，不影响排版、保存、预检或复制；
- 本轮只调整更新提醒层，不修改文章数据、主题、Markdown、链接、代码、公式或微信发布渲染器。

## V4.1.9 — 2026-08-26

> 自动回归与公开构建已通过；真实微信里的外部链接点击状态仍需按“粘贴、保存草稿、重新打开、手机预览”人工确认。

- 自动识别“官方核验入口、参考资料、资料来源”等纯链接列表，生成独立参考资料组件，不再套入圈号卡片；
- 普通列表中的 Markdown 链接保持列表语义，不再压成带完整长网址的粗体文字；
- 微信清洁复制尝试保留 `http/https` 安全链接，并显示短域名兜底；发布预检可以核对链接数量、名称和顺序；
- 参考资料与普通链接增加移动端强制换行，长链接不再撑坏卡片；
- 更新中心将“应用版本”和“内容包版本”分开判断，可以只提示其中一类更新；
- 免费版与创作者工作台使用独立内容包版本，更新提示不会把内容包误写成应用升级；
- 保留每 24 小时最多自动检查一次、不上传文章、网络失败不影响排版与复制的原有边界；
- 免费版、创作者工作台、GitHub 在线版与固定最新版下载共用同一链接兼容内核；
- 微信可能在粘贴或保存后改变外部链接可点击状态，因此短域名兜底和真实微信终验仍保留。

## V4.1.8 — 2026-08-25

- 第一次使用统一为“① 粘贴全文并排版 → ② 生成排版 → ③ 检查并复制到微信”，默认设置即可完成；
- 打开排版弹窗时不再自动读取剪贴板，只有主动点击“从剪贴板粘贴”才读取；
- 修复段中图片顺序、行内公式与价格符号区分、裸链接中文标点等 Markdown 技术内容边界；
- 微信输出审计新增区块内重复、清洗前后增删文字、组件类型缺失和纵向卡片／真表格表头缺失阻断；
- 统一新文章入口和操作提示，示例无需手动删除；复制动作会先执行发布检查，有内容阻断项时不会继续复制；
- 在线版成为推荐主入口，同一个地址持续提供最新版；离线 HTML 和 ZIP 保留为断网或长期留存备用；
- 明确在线使用不等于云同步，换电脑、换浏览器或清理数据前需下载文章备份 JSON；
- 明确区分排版器文件、文章备份 JSON 和微信复制页，补齐离线版“先备份 → 打开新版 → 恢复核对”的升级说明。

## V4.1.7 — 2026-08-25

- 修复短标题、文件名或工具名在长句中正常重现时，被误判为“微信输出重复”并阻断一键排版的问题；
- 微信输出完整性检查改为逐区块审计，实际丢字、多出区块或顺序变化仍会安全阻断；
- 旧版纯文本审计兼容路径会计入短语在其他字段中的合法出现，不再用“字段数量”冒充“全文子串次数”；
- 增加 `1→3 / 1→2 / 1→4` 短语嵌套、代码语言和表头重现回归测试。

## V4.1.6 — 2026-08-22

- 新增轻量检查更新：点击左上角版本号，可查看当前版本与最新版本；
- 启动后每 24 小时最多静默检查一次，只读取公开 `version.json`，不上传文章、标题、作者或草稿；
- 新版本提示先引导导出项目备份，离线 HTML 只提供下载入口，不会自动覆盖当前文件；
- 增加固定在线地址和固定最新下载文件名，以后分享同一个地址即可；
- 网络失败或断网时不影响编辑、自动保存、排版、预检和复制。

## V4.1.5 — 2026-08-22

- 将一键排版解析器升级为内嵌、固定版本的本地 Markdown Token 解析，不依赖 CDN；
- 新增代码块与公式块，组件总数从 31 升级为 33；
- 围栏代码逐字符保留缩进、Tab、空行、特殊符号和尾随换行，代码里的 Markdown/HTML 不会被误识别或执行；
- 支持行内代码、行内公式、链接、Markdown 图片、任务清单、分割线和可变列表格；
- 公式完整保留 LaTeX，并进入公式图片清单；公众号稳定复制默认阻断未处理公式，避免静默变成普通文字；
- 修复真实生成时把公式方括号、链接 URL、纵向表格标签误判为“原文被删除”的问题；
- 章节标题同时识别半角 `|` 与全角 `｜` 编号前缀，避免自动编号后重复显示；
- 未闭合代码围栏或块公式会显示原始行号并停止生成，当前草稿不会被修改；
- 免费版、创作者工作台版与 GitHub 在线版共用同一解析和微信发布内核。

## V4.1.4 — 2026-08-21（维护记录，未独立发布）

- 一键排版在没有品牌文末时，自动添加通用三连提示、关注提示和感谢页脚；
- 已有明确固定文末或定制文末时优先原样保留，不重复追加；
- “连同固定品牌文末一起替换”仍可生成真正无落尾的文章；
- 追加模式不自动插入落尾，避免合并文章时把文末夹在中间；
- 预览摘要和完成提示会明确说明本次是保留现有文末、添加通用落尾，还是不添加落尾。

## V4.1.3 — 2026-08-20

- 修复“一键排版只有普通正文、缺少卡片和背景”的结构断层；
- 新增版式配方层，按文章内容自动组装导语、重点、方法、总结和结尾；
- 默认使用均衡版式，可在“更多设置”切换清爽或丰富；
- 默认只替换正文并保留已定制的文末组件，仍可显式整篇替换；
- 生成前后执行原文完整性检查，发现删除、重复或顺序变化时停止修改；
- 修复 `**摘要：**`、`**关键词：**` 被误识别为列表的问题。

视觉主题负责颜色与标题风格，版式配方负责内容区块结构；两者现已分开工作。微信最终效果仍需完成粘贴、保存草稿、重新打开和手机预览。

## V4.1.2 — 2026-08-20

- 新增“一键排版文章”：读取剪贴板失败时可手动粘贴；所有处理均在当前浏览器完成；
- 默认替换上次草稿，可选追加到文末，整次生成可一步撤销；
- 自动识别文章标题、Markdown 小标题、段落、引用、列表、强调句和明确步骤；
- 自动判断教程实操、观点文章、故事复盘、清单盘点或通用文章，并匹配安全主题；
- 空白页不再是白屏死路；恢复旧草稿时明确显示区块数量，并提供“新文章一键排版”；
- 明确区分“排版样式（不改正文）”与“新建文章骨架（会替换正文）”。

仍需在微信后台完成：粘贴、保存草稿、重新打开和手机预览。

## V4.1.1 — 2026-08-20

首次 GitHub 开源发布：

- 35 套主题、16 套文章结构与 31 个内容区块；
- 微信独立发布渲染器，不直接复制编辑器状态；
- 非数据组件不使用表格模拟布局；
- 图片与链接清单、真实表格高风险提示；
- 静态发布预检、清洁富文本复制；
- 本地草稿、快照和项目 JSON 备份；
- 物理移除内部 QA、授权生成器和所有非公开版本数据；
- 新增 GitHub Pages 在线体验与 MPL-2.0 开源许可。

本地测试不等于微信最终验收。正式发布前仍需完成粘贴、保存草稿、重新打开和手机预览。
